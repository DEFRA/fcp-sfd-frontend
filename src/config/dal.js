export const dalConfig = {
  dalConfig: {
    endpoint: {
      doc: 'API endpoint to retrieve data from the data access layer (DAL)',
      format: 'String',
      default: null,
      env: 'DAL_ENDPOINT'
    }
  }
}

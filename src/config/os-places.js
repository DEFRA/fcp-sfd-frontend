export const osPlacesConfig = {
  osPlacesConfig: {
    clientId: {
      doc: 'Client ID for authenticating with OS Places',
      format: String,
      default: null,
      env: 'OS_PLACES_CLIENT_ID'
    }
  }
}

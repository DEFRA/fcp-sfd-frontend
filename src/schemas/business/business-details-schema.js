/**
 * Central map of business detail schemas.
 *
 * Schemas for the business details view/validation page are provided by the
 * shared engine package to avoid duplication with the internal frontend.
 */

import { schemas } from '@defra/fcp-sfd-frontend-engine'

export const businessDetailsSchema = schemas.business.details

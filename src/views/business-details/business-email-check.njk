{% extends 'common/layout.njk' %}

{% from "govuk/components/summary-list/macro.njk" import govukSummaryList %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "common/navigation/back-link.njk" import appBackLink %}
{% from "common/sub-header/macro.njk" import subHeader %}

{% set pageTitle = "Check your business email address is correct before submitting" %}
{% set metaDescription = "Check the email address for your business is correct." %}

{% block beforeContent %}
    {{ subHeader(
    businessName="Agile Farm Ltd", 
    sbi="123456789", 
    userName="Alfred Waldron"
) }}

    {{ appBackLink() }}
{% endblock %}

{% block content %}
<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <form method="post" novalidate>
      <div class="govuk-form-group">
        <h1 class="govuk-heading-l">
          {{ pageTitle }}
        </h1>

        {{ govukSummaryList({
          rows: [
            {
              key: {
                text: "Business email address"
              },
              value: {
                text: businessEmail
              },
              actions: {
                items: [
                  {
                    href: "/business-email-change",
                    text: "Change",
                    visuallyHiddenText: "business email"
                  }
                ]
              }
            }
          ]
        }) }}
      </div>

      {{ govukButton({
        text: "Submit"
      }) }}

      <p class="govuk-body">
        <a href="/business-details" class="govuk-link govuk-link--no-visited-state">Cancel</a>
      </p>
    </form>
  </div>
</div>
{% endblock %}
